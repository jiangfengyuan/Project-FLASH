package com.flash.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * 品牌 Seed Color（对应 Web 版主色调 #4D96FF）。
 * 当前 UI 采用「插画 × 玻璃拟态」风格（见 ui/theme/glass/），
 * 强调色为插画风紫 #6C5CE7；Seed 保留用于品牌标识类场景。
 */
val BrandSeed = Color(0xFF4D96FF)
