package com.flash.app.ui.theme

import androidx.compose.material3.Typography

/**
 * MD3 默认 Typography 即符合 PRD 3.3.2 的字体层级规范
 *（Display/Headline/Title/Body/Label 五组，sp 相对单位，支持系统字体缩放）。
 * 后续如需品牌字体可在此替换 FontFamily。
 */
val FlashTypography = Typography()
