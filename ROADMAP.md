# Flash 一闪 — 未来开发计划

> 版本：Alpha v6 整理后现状
> 更新日期：2026-07-13

---

## 一、项目现状速览

**产品定位**：本地优先的移动端灵感 / 日志 / 情绪记录应用。

**已完成功能**：

| 模块                        | 状态        | 说明                                                                                   |
| --------------------------- | ----------- | -------------------------------------------------------------------------------------- |
| Log Stream（首页输入）      | ✅ 可用     | 文本输入、语音转写、分类保存                                                           |
| Log Flow（日志管理）        | ✅ 可用     | 搜索、筛选、编辑、删除、转移至灵感                                                     |
| Idea Flow（灵感池）         | ✅ 可用     | 按时间分组、重要性标记                                                                 |
| Calendar（日历聚合）        | ✅ 可用     | 日志与情绪聚合、按日期查看                                                             |
| Current Emotion（情绪记录） | ✅ 可用     | 拖拽等级、子情绪标签、彩带动画                                                         |
| Android 打包                | ✅ 已配置   | release 签名、ProGuard、versionCode/versionName 自动化已配置；keystore 需本地 JDK 生成 |
| iOS 打包                    | ✅ 基线完成 | Capacitor iOS 工程已添加，竖屏、状态栏、启动图背景色已配置；签名团队待后续设置         |
| 数据同步/备份               | ❌ 未开始   | 仅 localStorage 本地持久化                                                             |

**技术栈**：React 19 + TypeScript 5.9 + Vite 8 + Tailwind CSS 3 + Framer Motion + Zustand + Capacitor 8。

**当前工程健康度**：

- 构建、类型检查、Lint、单测（132 个）全部通过。
- 已完成一轮结构与配置标准化（docs/、.gitattributes、依赖清理、Bug 修复）。
- 仍存在的短板：页面目录结构未完全统一、大体积静态资源未压缩、无 iOS 工程、无数据备份机制。

---

## 二、开发路线图

### Phase 1：工程收尾与多端基线（第 1–2 周）

目标：把项目从“可运行”推进到“可发布”的基线状态。

| 任务                      | 优先级 | 具体工作                                                                                                                                                    | 验收标准                                                                           |
| ------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| 统一页面目录结构          | P1     | 将 `LogFlow.tsx`、`Calendar.tsx` 也改为文件夹式，例如 `LogFlow/index.tsx`、`Calendar/index.tsx`，与 `LogStream/`、`IdeaFlow/`、`CurrentEmotion/` 保持一致。 | 所有页面均为 `pages/XXX/index.tsx` 或 `pages/XXX/XXX.tsx` 形式，无孤立单文件页面。 |
| 压缩 / 替换大体积静态资源 | P1     | `public/emotion-icons.png`（约 2.1 MB）、`public/user-placeholder.png`（约 1.4 MB）转 WebP 或压缩为 ≤ 300 KB 的 PNG。                                       | 产物总体积下降 ≥ 1 MB，视觉上无可见质量损失，`npm run build` 通过。                |
| 清理 Demo 数据            | P2     | 将 `logStore.ts`、`emotionStore.ts` 中内嵌的 DEMO 数据抽离到 `src/data/demo.ts`，并提供 `useDemoData()` 开关，首次启动时空数据即可。                        | 空数据启动不报错，Demo 数据仅用于开发/测试。                                       |
| 收敛 Vitest 实验性警告    | P2     | 在 `vitest.config.ts` 中显式配置 `localStorage` 或升级 jsdom 设置，消除 `localStorage is not available` 警告。                                              | `npm run test:run` 无 Node 实验性警告。                                            |
| 补充 iOS 工程             | P2     | `npx cap add ios`，检查 CocoaPods 依赖，生成 `ios/` 目录并提交（注意 xcuserdata 等需 gitignore）。                                                          | 可在 macOS 上 `npx cap open ios` 正常打开 Xcode 工程。                             |

### Phase 2：核心体验打磨（第 3–5 周）

目标：提升日常使用的稳定性与流畅度。

| 任务                 | 优先级    | 具体工作                                                                                     | 验收标准                                                                 |
| -------------------- | --------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| 数据导入 / 导出      | ✅ 已完成 | 在设置页提供“导出 JSON 备份”与“导入 JSON 备份”功能，覆盖日志与情绪数据。                     | 导出的 JSON 可完整还原到另一设备/浏览器，`import` 后页面刷新数据不丢失。 |
| 本地数据加密（可选） | P2        | 使用 `crypto-js` 或 Web Crypto API 对 localStorage 中的敏感字段做 AES 加密，密码由用户设置。 | 未输入密码时 localStorage 内容不可读，输入正确密码后正常加载。           |
| 情绪趋势统计         | ✅ 已完成 | 在 Current Emotion 或新增“统计”页中，展示近 7/30 天情绪等级折线图、标签分布。                | 有至少一种可视化图表（可引入 `recharts` 或轻量 SVG 自绘）。              |
| 搜索与筛选增强       | ✅ 已完成 | Log Flow 支持按日期范围、情绪标签、关键词联合筛选；结果支持排序。                            | 复杂筛选组合下结果正确，性能在 1000 条日志内无明显卡顿。                 |
| 语音输入鲁棒性       | P2        | 处理 Web Speech API 不可用场景（非 HTTPS、部分 Android WebView），给出友好降级提示。         | 在不支持设备上隐藏语音按钮或显示“当前环境不支持语音输入”。               |
| 离线可用性检测       | P2        | 增加网络状态提示，但产品本身始终可离线使用。                                                 | 无网络时核心功能不受影响。                                               |

### Phase 3：移动端发布准备（第 6–8 周）

目标：完成 Android / iOS 双端打包与发布前检查。

| 任务                | 优先级      | 具体工作                                                                                                                                                                  | 验收标准                                                                                              |
| ------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Android 发布配置    | ✅ 已完成   | 配置 release 签名 keystore（由 `scripts/generate-android-keystore.sh` 生成）、启用代码压缩（`minifyEnabled true` + ProGuard 规则）、设置 `versionCode` 与 `versionName`。 | 可构建出 signed release APK/AAB，`android/app/build.gradle` 中配置完整。                              |
| iOS 发布配置        | ✅ 基线完成 | 添加 Capacitor iOS 工程、配置 Bundle Identifier、竖屏、状态栏颜色、启动图背景色；签名团队与自定义图标待后续。                                                             | 可 `npx cap open ios` 打开 Xcode 工程；真机/TestFlight 需配置签名团队。                               |
| 状态栏 / 安全区适配 | ✅ 已完成   | 集成 StatusBar 插件、处理刘海屏底部导航、键盘弹起时输入框被遮挡。                                                                                                         | 在 iPhone 14/15 系列与主流 Android 机型上 UI 无遮挡。                                                 |
| 返回键 / 手势适配   | ✅ 已完成   | Android 物理返回键映射到页面返回；iOS 侧滑返回不冲突。                                                                                                                    | 在双端真机上返回行为符合平台习惯。                                                                    |
| 添加 Settings 页面  | ✅ 已完成   | 集中管理主题、数据导出/导入、关于、清除缓存、版本号。                                                                                                                     | 设置入口可访问，所有功能可用。                                                                        |
| 深色模式完整适配    | ✅ 已完成   | 检查所有页面在 dark 模式下的可读性，补齐缺失的 `dark:` 样式；新增主题模式切换（跟随系统/始终深色/始终浅色）。                                                             | 切换系统深色模式或手动切换主题后无明显色块/文字不可读问题。                                           |
| 平台视觉适配        | ✅ 已完成   | 通过 CSS/Tailwind token 区分 Android Material 3 与 iOS Liquid Glass 视觉风格；Web 预览按 UA 自动切换。                                                                    | Android 端显示 Material 3 圆角/阴影/Indigo 强调色；iOS 端显示 Liquid Glass 较小圆角/Apple 蓝/强模糊。 |

### Phase 4：功能扩展与生态（第 9–14 周）

目标：在保持轻量定位的前提下，逐步增加高价值功能。

| 任务               | 优先级 | 具体工作                                                                                                        | 验收标准                                              |
| ------------------ | ------ | --------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 图片 / 附件记录    | P1     | 支持在日志中附加 1-3 张图片，使用 Capacitor Camera 插件或 `<input type="file">`，本地以 Base64 或文件路径存储。 | 可拍照/选图并随日志展示，Android/iOS 均可正常调用。   |
| 标签系统           | P1     | 自定义标签替代固定分类，提供标签云、自动补全。                                                                  | 日志可打多个标签，Idea Flow / Log Flow 可按标签筛选。 |
| 每日回顾 / 提醒    | P2     | 本地推送提醒（Capacitor Local Notifications），支持设置每日记录提醒时间。                                       | 在真机上可收到本地通知，点击后打开应用。              |
| 多语言支持         | P2     | 提取中文文案到 `locales/zh-CN.json`，预留 `en-US` 结构，使用 i18n 库（如 `i18next`）。                          | 所有用户可见文案走 i18n，新增语言只需翻译文件。       |
| 数据迁移与版本兼容 | P2     | 为 Zustand persist 增加版本号与 migration，防止后续数据结构变更导致旧数据加载失败。                             | 修改数据模型后旧用户数据可自动迁移。                  |
| 桌面端适配（可选） | P3     | 微调大屏布局，或评估是否用 Capacitor 暂不支持的桌面方案。                                                       | 在 768px 以上屏幕布局不崩坏。                         |

### Phase 6：原生重写（2026-07 启动，与 Web 版并行）

目标：以原生技术栈重写双端，获得完整平台体验。**Android 先行**（工具链稳定、Kotlin 逻辑层未来可抽取为 KMP 共享模块），iOS（SwiftUI）随后。

| 任务 | 优先级 | 具体工作 | 验收标准 |
| --- | --- | --- | --- |
| Android 原生工程骨架 | P0 | `native-android/`：Kotlin 2.2 + Jetpack Compose + Material 3 + Room（表结构对齐现有 SQLite schema）+ MVVM/StateFlow | `./gradlew assembleDebug` 通过，模拟器可运行 |
| 情绪记录主流程 | P0 | MD3 离散滑块（-3..+3）、负面子情绪 Chip、备注、历史列表；`EmotionStats` 算法已移植 | 数据落 Room，杀进程重启不丢 |
| 日志 / 灵感模块 | P1 | 移植 Log Stream / Log Flow / Idea Flow，含搜索筛选与重要性标记 | 功能与 Web 版对齐 |
| 日历与统计图表 | P1 | 移植 Calendar 聚合页与情绪趋势图（克制动画） | 图表渲染正确 |
| 设置与导入导出 | P2 | JSON 导入导出，格式与 Web 版互通 | Web 导出文件可在原生端导入 |
| MD3 主题精调 | P2 | 用 Material Theme Builder 从 Seed #4D96FF 生成精确 Tonal Palette；动态取色已支持 | 色彩对比度符合 WCAG |
| iOS 原生（SwiftUI） | P2 | 待 Android 版稳定后启动；届时评估将 Kotlin 逻辑层抽为 KMP 共享模块，iOS 只写 SwiftUI 界面 | — |

### Phase 5：长期优化与运营（第 15–24 周）

目标：建立可持续迭代的能力。

| 任务                   | 优先级 | 具体工作                                                                                           | 验收标准                     |
| ---------------------- | ------ | -------------------------------------------------------------------------------------------------- | ---------------------------- |
| 端到端测试             | P2     | 引入 Playwright 或 Appium，覆盖核心用户路径（记录日志 → 查看日历 → 记录情绪 → 导出数据）。         | 至少 3 条 E2E 用例稳定通过。 |
| 性能监控               | P2     | 增加 FPS / 启动时长 / 包体积的 CI 告警阈值，避免回归。                                             | CI 中新增体积/性能检查步骤。 |
| 用户反馈通道           | P3     | 在设置页增加邮件/表单反馈入口。                                                                    | 可收集用户反馈并追踪。       |
| 云同步（可选，重决策） | P3     | 评估是否需要后端同步；如需要，优先使用用户自托管（WebDAV / iCloud / Google Drive）而非自建服务器。 | 完成方案文档与 PoC。         |

---

## 三、里程碑与发布节奏

| 里程碑         | 预计时间   | 交付物                                   | 发布建议                                      |
| -------------- | ---------- | ---------------------------------------- | --------------------------------------------- |
| M1：工程基线   | 第 2 周末  | 统一目录、资源压缩、iOS 工程、无警告测试 | 内部技术评审，不对外发布                      |
| M2：体验版     | 第 5 周末  | 导入导出、情绪统计、搜索增强             | Android 内测 APK / TestFlight 内测            |
| M3：正式版 1.0 | 第 8 周末  | 双端签名、设置页、深色模式、平台适配     | 上架 Google Play / App Store（或 TestFlight） |
| M4：功能扩展版 | 第 14 周末 | 图片附件、标签系统、本地提醒、多语言     | 1.1/1.2 迭代更新                              |
| M5：稳定运营版 | 第 24 周末 | E2E 测试、性能监控、云同步方案           | 持续小版本迭代                                |
| M6：原生 Android Alpha | 原生启动后第 4 周 | 情绪记录 + 日志主流程可用的原生 APK | 与 Capacitor 版并行内测，收集对比反馈 |
| M7：原生 macOS Alpha ✅ | 2026-08（已完成） | SwiftUI + SwiftData 九模块全量（首页/探索/记录流/情绪/日历/统计/设置/欢迎/复用组件）、universal2 双架构、JSON 备份与 Android/Web 互通、38 个单测全过 | 本地 adhoc 构建内测，对外分发需 Developer ID + 公证；后续候选：menu bar extra 快速记录、iCloud 同步、原生 iOS 版共享模型 |

---

## 四、技术债务与架构演进

### 4.1 当前仍需偿还的债务

1. **页面目录未完全统一**：`LogFlow.tsx`、`Calendar.tsx` 与文件夹式页面混用。
2. **Demo 数据内嵌**：污染生产代码，且会让首次启动看起来“有内容”。
3. **大体积静态资源**：影响首次加载与 APK 体积。
4. **缺少数据迁移机制**：后续一旦修改 Store 结构，老用户数据可能损坏。
5. **Android release keystore 待生成**：配置已就绪，需本地 JDK 环境运行 `scripts/generate-android-keystore.sh`。

### 4.2 推荐架构演进方向

```
Phase 1（现在）
  Web App + Capacitor Android
  └── localStorage 持久化

Phase 2
  Web App + Capacitor Android/iOS
  └── localStorage + JSON 导入/导出

Phase 3（可选）
  Web App + Capacitor Android/iOS
  └── localStorage + 用户自托管同步（WebDAV / iCloud / Google Drive）
```

**保持本地优先**：除非有非常明确的云同步需求，否则优先把数据留在用户设备，降低合规与运维成本。

---

## 五、风险与应对

| 风险                                         | 影响 | 应对策略                                                          |
| -------------------------------------------- | ---- | ----------------------------------------------------------------- |
| iOS 上架审核严格（本地存储、内容审核）       | 高   | 提前准备隐私政策、说明数据不联网、不包含用户生成内容分享功能。    |
| Web Speech API 在 Capacitor WebView 中不稳定 | 中   | 提供明确降级，语音仅作为“可选增强”。                              |
| localStorage 容量限制（约 5-10 MB）          | 中   | 图片采用文件路径引用而非全量 Base64；大量数据时提示用户导出备份。 |
| 跨平台 UI 细节差异                           | 中   | 每周至少一次真机测试，优先解决安全区、键盘、返回键问题。          |
| 后续数据模型变更导致旧数据损坏               | 高   | Phase 2 即引入 Zustand persist migration。                        |

---

## 六、下一步可立即执行的动作

如果接下来只有 1–2 小时，建议按以下顺序：

1. **统一页面目录**：把 `LogFlow.tsx` 和 `Calendar.tsx` 改为文件夹结构。
2. **压缩静态资源**：`emotion-icons.png` 与 `user-placeholder.png` 转 WebP。
3. **抽离 Demo 数据**：新建 `src/data/demo.ts`，Store 默认空数据。

这三项改动小、收益高，能显著改善工程整洁度与包体积。

---

## 七、附录：推荐依赖清单（按需引入）

| 功能        | 推荐依赖                         | 备注                        |
| ----------- | -------------------------------- | --------------------------- |
| 图表        | `recharts` 或自研 SVG            | 情绪趋势统计                |
| 本地通知    | `@capacitor/local-notifications` | 每日提醒                    |
| 相机 / 相册 | `@capacitor/camera`              | 图片附件                    |
| 文件系统    | `@capacitor/filesystem`          | 导出/导入本地文件           |
| 分享        | `@capacitor/share`               | 导出后分享 JSON             |
| i18n        | `i18next` + `react-i18next`      | 多语言                      |
| 加密        | `crypto-js` 或 Web Crypto API    | 本地数据加密                |
| E2E 测试    | `playwright`                     | Web 端 E2E；移动端用 Appium |

---

_本计划应根据实际进度每月回顾一次，优先完成 P0/P1 项，P2/P3 可根据用户反馈与资源情况调整顺序。_
