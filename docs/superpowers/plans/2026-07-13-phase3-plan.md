# Phase 3 移动端发布准备实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 Android release 签名与构建配置、添加 iOS 工程基线、实现状态栏/安全区/返回键/键盘适配、补齐深色模式，并通过 CSS/Tailwind 设计 token 与平台检测让 Android 呈现 Material 3 风格、iOS 呈现 Liquid Glass 风格，使应用达到可构建 signed APK/AAB 与可上架 iOS 的工程状态。

**Architecture:** 在现有 React + Capacitor 8 基础上，通过 Gradle 配置与 Capacitor 插件完成原生发布与平台适配；前端侧使用 Tailwind `env(safe-area-inset-*)` 与独立 theme store 处理安全区和主题切换，保持业务组件改动最小化。平台视觉通过 `src/lib/platform.ts` 检测并在 `<html>` 上打上 `platform-android` / `platform-ios` 类名，由 `src/index.css` 提供两套设计 token，组件直接引用变量或按平台类覆盖样式。

**Tech Stack:** React 19 + TypeScript 5.9 + Vite 8 + Tailwind CSS 3 + Capacitor 8 + Gradle + Xcode

## Global Constraints

- 竖屏 Portrait -only（Android + iOS）。
- iOS 仅做工程基线，不配置签名团队。
- Android 使用自动生成的内测 keystore，密码不写入仓库，通过环境变量注入。
- `versionName` 与 `versionCode` 必须跟随 `package.json` 的 `version`。
- 所有新增代码必须通过 `npm run lint && npm run format:check && npx tsc -b --noEmit && npm run test:run && npm run build`。
- Capacitor 插件调用需静默降级，避免 Web 预览崩溃。
- **不引入 Jetpack Compose / SwiftUI 等原生 UI 框架**，原生框架重构列为 Phase 5 评估项。

---

## File Structure

| 文件/目录 | 职责 |
|-----------|------|
| `android/app/build.gradle` | Android release 签名、minify、versionCode/versionName 配置 |
| `android/app/proguard-rules.pro` | ProGuard keep 规则 |
| `android/app/flash-release.keystore` | 生成的内测签名密钥库（提交到仓库，但仅用于内测） |
| `android/release-signing.env.example` | 环境变量模板 |
| `android/app/src/main/AndroidManifest.xml` | 屏幕方向、allowBackup 等原生声明 |
| `ios/` | Capacitor iOS 工程目录 |
| `ios/App/App/Info.plist` | iOS 方向、状态栏、Bundle ID 配置 |
| `ios/App/App/Assets.xcassets/` | iOS 应用图标与启动图资源 |
| `capacitor.config.ts` | Capacitor 全局配置，新增 statusBar/keyboard 插件配置 |
| `src/main.tsx` | 应用启动时初始化 statusBar、keyboard 等原生插件 |
| `src/stores/themeStore.ts` | 主题模式状态（system/dark/light）与持久化 |
| `src/pages/Settings/index.tsx` | 新增主题模式切换入口 |
| `src/App.tsx` | 监听系统主题变化、Android 返回键事件、写入平台类名 |
| `src/lib/platform.ts` | 平台检测（`Capacitor.getPlatform()` + UA fallback） |
| `src/lib/__tests__/platform.test.ts` | 平台检测单元测试 |
| `src/hooks/useSafeArea.ts` | 读取并注入安全区 CSS 变量的 hook |
| `src/hooks/useBackButton.ts` | 处理 Android 物理返回键的 hook |
| `src/index.css` | 安全区变量、深色模式兜底样式、平台设计 token |
| `src/components/BottomNav.tsx` | 底部导航，应用平台 token |
| `src/components/LiquidGlassCard.tsx` | 玻璃卡片，通过 `.liquid-glass` 引用平台 token |
| `src/pages/LogStream/InputArea.tsx` | 输入区域，应用平台 token |
| `src/pages/LogFlow/FilterDrawer.tsx` | 筛选抽屉，应用平台 token |

---

## Task 1: Android Release 签名配置

**Files:**
- Create: `android/app/flash-release.keystore`
- Create: `android/release-signing.env.example`
- Modify: `android/app/build.gradle:1-120`
- Modify: `.gitignore`

**Interfaces:**
- Produces: signed release APK/AAB via `./gradlew assembleRelease` / `./gradlew bundleRelease`

- [ ] **Step 1: 生成内测 keystore**

Run:
```bash
cd android/app
keytool -genkey -v -keystore flash-release.keystore -alias flash-release -keyalg RSA -keysize 2048 -validity 10950 -storepass flashdev -keypass flashdev -dname "CN=Flash Dev"
cd ../..
```
Expected: `android/app/flash-release.keystore` created.

- [ ] **Step 2: 创建环境变量模板**

Create `android/release-signing.env.example`:
```bash
export FLASH_RELEASE_STORE_FILE=flash-release.keystore
export FLASH_RELEASE_STORE_PASSWORD=flashdev
export FLASH_RELEASE_KEY_ALIAS=flash-release
export FLASH_RELEASE_KEY_PASSWORD=flashdev
```

- [ ] **Step 3: 修改 build.gradle 添加 release 签名配置**

Modify `android/app/build.gradle`, replace the `android { ... }` block to include:
```gradle
android {
    namespace "com.flash.app"
    compileSdk rootProject.ext.compileSdkVersion

    defaultConfig {
        applicationId "com.flash.app"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "0.1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    signingConfigs {
        release {
            storeFile file(String.valueOf(System.getenv('FLASH_RELEASE_STORE_FILE') ?: 'flash-release.keystore'))
            storePassword System.getenv('FLASH_RELEASE_STORE_PASSWORD') ?: 'flashdev'
            keyAlias System.getenv('FLASH_RELEASE_KEY_ALIAS') ?: 'flash-release'
            keyPassword System.getenv('FLASH_RELEASE_KEY_PASSWORD') ?: 'flashdev'
        }
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.release
        }
    }
}
```

- [ ] **Step 4: 将 keystore 密码环境变量名加入 .gitignore 提示**

Append to `.gitignore`:
```gitignore
# Android signing secrets
android/release-signing.env
```

- [ ] **Step 5: 验证签名配置**

Run:
```bash
npm run build
npx cap sync android
cd android && ./gradlew assembleRelease
```
Expected: Build succeeds, outputs `android/app/build/outputs/apk/release/app-release.apk`.
Verify signed:
```bash
apksigner verify android/app/build/outputs/apk/release/app-release.apk
```
Expected: `WARNING: ...` or `Verified` (depends on apksigner version).

- [ ] **Step 6: Commit**

```bash
git add android/app/build.gradle android/app/flash-release.keystore android/release-signing.env.example .gitignore
git commit -m "build(android): add release signing config with dev keystore"
```

---

## Task 2: Android 版本号自动化

**Files:**
- Create: `scripts/android-version.mjs`
- Modify: `android/app/build.gradle`
- Modify: `package.json`
- Test: `src/lib/__tests__/version.test.ts`

**Interfaces:**
- Produces: `versionCode` integer computed from `package.json` version
- Produces: `versionName` string from `package.json` version

- [ ] **Step 1: 编写版本号计算脚本**

Create `scripts/android-version.mjs`:
```js
import { readFileSync } from 'fs';
import { resolve } from 'path';

const pkgPath = resolve(process.cwd(), 'package.json');
const pkg = JSON.parse(readFileSync(pkgPath, 'utf-8'));
const [major, minor, patch] = pkg.version.split('.').map(Number);
const versionCode = major * 10000 + minor * 100 + patch;

process.stdout.write(`${versionCode}\n`);
```

- [ ] **Step 2: 在 build.gradle 中读取版本号**

Modify `android/app/build.gradle`:
```gradle
def getVersionCode() {
    def code = new ByteArrayOutputStream()
    exec {
        commandLine 'node', '../../scripts/android-version.mjs'
        standardOutput = code
    }
    return code.toString().trim().toInteger()
}

def getVersionName() {
    def name = new ByteArrayOutputStream()
    exec {
        commandLine 'node', '-e', "console.log(require('../../package.json').version)"
        standardOutput = name
    }
    return name.toString().trim()
}

android {
    ...
    defaultConfig {
        ...
        versionCode getVersionCode()
        versionName getVersionName()
    }
}
```

- [ ] **Step 3: 添加测试验证 versionCode 计算**

Create `src/lib/__tests__/version.test.ts`:
```ts
import { describe, it, expect } from 'vitest';

function versionToCode(version: string): number {
  const [major, minor, patch] = version.split('.').map(Number);
  return major * 10000 + minor * 100 + patch;
}

describe('versionToCode', () => {
  it('maps semantic version to integer code', () => {
    expect(versionToCode('0.1.0')).toBe(100);
    expect(versionToCode('1.2.3')).toBe(10203);
    expect(versionToCode('2.11.7')).toBe(21107);
  });
});
```

- [ ] **Step 4: 运行测试与构建**

Run:
```bash
npm run test:run -- src/lib/__tests__/version.test.ts
npm run build
cd android && ./gradlew assembleRelease
```
Expected: versionName in APK matches `package.json` version.

- [ ] **Step 5: Commit**

```bash
git add scripts/android-version.mjs src/lib/__tests__/version.test.ts android/app/build.gradle package.json
git commit -m "build(android): derive versionCode and versionName from package.json"
```

---

## Task 3: Android ProGuard 规则

**Files:**
- Create: `android/app/proguard-rules.pro`
- Modify: `android/app/build.gradle`

**Interfaces:**
- Produces: valid ProGuard keep rules preventing release crash

- [ ] **Step 1: 创建 ProGuard 规则文件**

Create `android/app/proguard-rules.pro`:
```proguard
# Capacitor
-keep class com.getcapacitor.** { *; }
-keepclassmembers class com.getcapacitor.** { *; }

# Capacitor plugins
-keep class com.capacitorjs.plugins.** { *; }
-keepclassmembers class com.capacitorjs.plugins.** { *; }

# FileProvider
-keep class androidx.core.content.FileProvider { *; }

# Keep annotated classes
-keep @androidx.annotation.Keep class * { *; }
-keepclassmembers @androidx.annotation.Keep class * { *; }

# WebView / JS Bridge
-keepclassmembers class * {
    @android.webkit.JavascriptInterface <methods>;
}
```

- [ ] **Step 2: 确认 build.gradle 引用该文件**

Ensure `buildTypes.release` contains:
```gradle
proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
```

- [ ] **Step 3: 构建 release 并安装到模拟器验证**

Run:
```bash
cd android && ./gradlew assembleRelease
adb install -r app/build/outputs/apk/release/app-release.apk
```
Expected: App launches without crash, Splash hides, BottomNav works.

- [ ] **Step 4: Commit**

```bash
git add android/app/proguard-rules.pro android/app/build.gradle
git commit -m "build(android): add ProGuard keep rules for Capacitor"
```

---

## Task 4: iOS 平台基线与图标/启动图

**Files:**
- Create: `ios/` (via `npx cap add ios`)
- Modify: `ios/App/App/Info.plist`
- Modify: `ios/App/App/Assets.xcassets/AppIcon.appiconset/Contents.json`
- Create: icon PNGs under `ios/App/App/Assets.xcassets/AppIcon.appiconset/`
- Modify: `ios/App/App/LaunchScreen.storyboard`
- Modify: `capacitor.config.ts`

**Interfaces:**
- Produces: `npx cap open ios` opens Xcode project
- Produces: iOS app builds in Simulator with placeholder icon and launch screen

- [ ] **Step 1: 添加 iOS 平台**

Run:
```bash
npm install @capacitor/ios
npx cap add ios
npx cap sync ios
```
Expected: `ios/` directory created.

- [ ] **Step 2: 更新 Capacitor 配置**

Modify `capacitor.config.ts`:
```ts
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flash.app',
  appName: '一闪',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
  android: {
    backgroundColor: '#0a0e1a',
  },
  ios: {
    backgroundColor: '#0a0e1a',
    scheme: 'Flash',
  },
  plugins: {
    StatusBar: {
      style: 'DARK',
      backgroundColor: '#0a0e1a',
    },
    Keyboard: {
      resize: 'ionic',
    },
  },
};

export default config;
```

- [ ] **Step 3: 限制 iOS 方向为竖屏**

Modify `ios/App/App/Info.plist`, replace `UISupportedInterfaceOrientations` and `UISupportedInterfaceOrientations~ipad`:
```xml
<key>UISupportedInterfaceOrientations</key>
<array>
    <string>UIInterfaceOrientationPortrait</string>
</array>
<key>UISupportedInterfaceOrientations~ipad</key>
<array>
    <string>UIInterfaceOrientationPortrait</string>
</array>
```

- [ ] **Step 4: 保留默认 Capacitor 图标集**

`npx cap add ios` 已生成默认图标集。本次基线暂不替换为自定义图标，避免引入 ImageMagick/Sharp 等额外依赖；后续拿到正式设计稿后可直接覆盖 `ios/App/App/Assets.xcassets/AppIcon.appiconset`。

- [ ] **Step 5: 修改启动图背景色**

修改 `ios/App/App/LaunchScreen.storyboard`，将根视图背景色设为 `#0a0e1a`，与 capacitor.config.ts 一致。保持默认 Logo/Label 不变或删除以避免与 Splash 屏幕重复。

- [ ] **Step 6: 同步并验证**

Run:
```bash
npx cap sync ios
```
Open in Xcode:
```bash
npx cap open ios
```
Build for iPhone simulator (requires Xcode). If build fails due to signing, that is expected at this stage; note the error for later.

- [ ] **Step 7: Commit**

```bash
git add capacitor.config.ts ios/
git commit -m "feat(ios): add Capacitor iOS baseline with portrait-only and placeholder icons"
```

---

## Task 5: 平台视觉适配

**Files:**
- Create: `src/lib/platform.ts`
- Create: `src/lib/__tests__/platform.test.ts`
- Modify: `src/App.tsx`
- Modify: `src/index.css`
- Modify: `src/components/BottomNav.tsx`
- Modify: `src/components/LiquidGlassCard.tsx` (verify/augment class usage)
- Modify: `src/pages/LogStream/InputArea.tsx`
- Modify: `src/pages/LogFlow/FilterDrawer.tsx`

**Interfaces:**
- Produces: `getPlatform(): 'android' | 'ios' | 'web'`
- Produces: `<html class="platform-android">` or `<html class="platform-ios">`
- Produces: Material 3 (Android) / Liquid Glass (iOS) visual tokens consumed by components

- [ ] **Step 1: 创建平台检测工具**

Create `src/lib/platform.ts`:
```ts
import { Capacitor } from '@capacitor/core';

export type Platform = 'android' | 'ios' | 'web';

export function getPlatform(): Platform {
  const cap = Capacitor.getPlatform();
  if (cap === 'android') return 'android';
  if (cap === 'ios') return 'ios';

  // Web preview: sniff User Agent.
  const ua = typeof navigator !== 'undefined' ? navigator.userAgent : '';
  if (/Android/i.test(ua)) return 'android';
  if (/iPhone|iPad|iPod|Macintosh/i.test(ua)) return 'ios';

  // Default to iOS visual on unknown desktop/web.
  return 'ios';
}

export function isNative(): boolean {
  return Capacitor.isNativePlatform();
}
```

- [ ] **Step 2: 应用启动时写入平台类名**

Modify `src/App.tsx`, add import and effect:
```ts
import { getPlatform } from '@/lib/platform';

export default function App() {
  useEffect(() => {
    const platform = getPlatform();
    document.documentElement.classList.add(`platform-${platform}`);
    return () => {
      document.documentElement.classList.remove(`platform-${platform}`);
    };
  }, []);

  ...
}
```

- [ ] **Step 3: 在 index.css 定义平台设计 token**

Add the following blocks inside `src/index.css`, after the existing `.dark` block in `@layer base`:
```css
  .platform-android {
    --radius-card: 16px;
    --radius-sheet: 28px;
    --radius-button: 9999px;
    --color-primary: #6750a4;
    --shadow-card: 0 4px 20px rgba(0, 0, 0, 0.25), 0 1px 3px rgba(0, 0, 0, 0.15);
    --shadow-sheet: 0 -8px 32px rgba(0, 0, 0, 0.35);
    --ease-standard: cubic-bezier(0.2, 0, 0, 1);
    --ease-spring: cubic-bezier(0.2, 0, 0, 1);
    --backdrop-blur: blur(12px) saturate(130%);
    --nav-background: rgba(15, 23, 42, 0.85);
    --nav-blur: blur(16px);
  }

  .platform-ios {
    --radius-card: 12px;
    --radius-sheet: 22px;
    --radius-button: 12px;
    --color-primary: #0a84ff;
    --shadow-card: 0 2px 12px rgba(0, 0, 0, 0.15);
    --shadow-sheet: 0 -4px 24px rgba(0, 0, 0, 0.2);
    --ease-standard: cubic-bezier(0.32, 0.72, 0, 1);
    --ease-spring: cubic-bezier(0.32, 0.72, 0, 1);
    --backdrop-blur: blur(24px) saturate(180%);
    --nav-background: rgba(15, 23, 42, 0.6);
    --nav-blur: blur(32px) saturate(160%);
  }
```

Then update the Liquid Glass system to reference the tokens (with fallbacks for safety):
```css
.liquid-glass {
  background: rgba(255, 255, 255, 0.05);
  background-blend-mode: luminosity;
  backdrop-filter: var(--backdrop-blur, blur(24px) saturate(150%));
  -webkit-backdrop-filter: var(--backdrop-blur, blur(24px) saturate(150%));
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: var(--radius-card, 24px);
  box-shadow:
    inset 0 1px 1px rgba(255, 255, 255, 0.1),
    var(--shadow-card, 0 8px 32px rgba(0, 0, 0, 0.2));
}

.liquid-glass-sm {
  background: rgba(255, 255, 255, 0.05);
  background-blend-mode: luminosity;
  backdrop-filter: var(--backdrop-blur, blur(20px) saturate(140%));
  -webkit-backdrop-filter: var(--backdrop-blur, blur(20px) saturate(140%));
  border: 1px solid rgba(255, 255, 255, 0.12);
  border-radius: var(--radius-card, 16px);
  box-shadow:
    inset 0 1px 1px rgba(255, 255, 255, 0.08),
    var(--shadow-card, 0 4px 16px rgba(0, 0, 0, 0.15));
}

.liquid-glass-pill {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: var(--backdrop-blur, blur(16px) saturate(130%));
  -webkit-backdrop-filter: var(--backdrop-blur, blur(16px) saturate(130%));
  border: 1px solid rgba(255, 255, 255, 0.12);
  border-radius: var(--radius-button, 9999px);
}

.liquid-glass-input {
  background: rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-card, 16px);
}
```

Add platform-specific BottomNav overrides:
```css
.platform-android .bottom-nav {
  border-radius: 28px 28px 0 0;
  box-shadow: var(--shadow-sheet);
}

.platform-ios .bottom-nav {
  border-top: 1px solid rgba(255, 255, 255, 0.08);
  box-shadow: 0 -4px 24px rgba(0, 0, 0, 0.3);
}
```

- [ ] **Step 4: 更新 BottomNav 使用平台 token**

Modify `src/components/BottomNav.tsx`, replace the `liquid-glass-nav` class on the `<nav>` element:
```tsx
<nav
  className="fixed bottom-0 left-0 right-0 z-50 flex items-center justify-around h-16 pb-2 bottom-nav"
  style={{
    backgroundColor: 'var(--nav-background)',
    backdropFilter: 'var(--nav-blur)',
    WebkitBackdropFilter: 'var(--nav-blur)',
  }}
>
```

- [ ] **Step 5: 确认 LiquidGlassCard 继承 token**

`src/components/LiquidGlassCard.tsx` already applies `liquid-glass`. After Step 3 the radius/blur/shadow are token-driven, so no JSX change is required. Verify the component still renders with `className={cn('liquid-glass relative overflow-hidden', ...)}`.

- [ ] **Step 6: 更新 InputArea 使用平台 token**

In `src/pages/LogStream/InputArea.tsx`:
1. Replace the idle mic button inline shadow with the platform shadow token:
   ```tsx
   style={{
     boxShadow: 'var(--shadow-card)',
     ...
   }}
   ```
2. Replace the typing send button color:
   ```tsx
   className="w-8 h-8 rounded-full bg-[var(--color-primary)]/80 flex items-center justify-center disabled:opacity-30 active:scale-90 transition-transform"
   ```
3. The `liquid-glass-input` container now uses `var(--radius-card)` for its border radius.

- [ ] **Step 7: 更新 FilterDrawer 使用平台 token**

In `src/pages/LogFlow/FilterDrawer.tsx`:
1. Replace `rounded-t-3xl` with `rounded-t-[var(--radius-sheet)]` on the drawer container.
2. Replace the primary action button background `bg-blue-500/30` with `bg-[var(--color-primary)]/30`.
3. Replace `rounded-lg` on date inputs and quick date buttons with `rounded-[var(--radius-card)]`.

- [ ] **Step 8: Web 预览 UA 切换验证**

Run:
```bash
npm run dev
```
Open the local URL in Chrome, open DevTools → More tools → Network conditions, set User Agent to an iPhone string, reload, and confirm `<html class="platform-ios">`. Switch to an Android UA and confirm `<html class="platform-android">`. Verify visible differences:
- iOS: smaller card radius (`12px`), blue primary tint (`#0A84FF`), softer shadows.
- Android: larger card radius (`16px`), indigo primary tint (`#6750A4`), stronger shadows, capsule buttons.

- [ ] **Step 9: 添加平台检测单元测试**

Create `src/lib/__tests__/platform.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { getPlatform } from '@/lib/platform';

vi.mock('@capacitor/core', () => ({
  Capacitor: {
    getPlatform: () => 'web',
    isNativePlatform: () => false,
  },
}));

describe('getPlatform', () => {
  beforeEach(() => {
    vi.unstubAllGlobals();
  });

  it('defaults to ios on plain web', () => {
    expect(getPlatform()).toBe('ios');
  });

  it('sniffs Android UA', () => {
    vi.stubGlobal('navigator', { userAgent: 'Mozilla/5.0 (Linux; Android 14)' });
    expect(getPlatform()).toBe('android');
  });

  it('sniffs iOS UA', () => {
    vi.stubGlobal('navigator', { userAgent: 'Mozilla/5.0 (iPhone; CPU iPhone OS 17)' });
    expect(getPlatform()).toBe('ios');
  });
});
```

Run:
```bash
npm run test:run -- src/lib/__tests__/platform.test.ts
```
Expected: PASS.

- [ ] **Step 10: 明确原生 UI 框架边界**

Add a comment at the top of `src/lib/platform.ts` and a note in `docs/superpowers/specs/2026-07-13-phase3-design.md` section 2.3:
```ts
// Phase 3 platform visual adaptation uses CSS/Tailwind tokens only.
// Jetpack Compose / SwiftUI introduction is intentionally deferred to Phase 5.
```
No actual framework code is added.

- [ ] **Step 11: 运行前端质量门禁**

Run:
```bash
npm run lint
npm run format:check
npx tsc -b --noEmit
npm run test:run
npm run build
```
Expected: All green.

- [ ] **Step 12: Commit**

```bash
git add src/lib/platform.ts src/lib/__tests__/platform.test.ts src/App.tsx src/index.css src/components/BottomNav.tsx src/pages/LogStream/InputArea.tsx src/pages/LogFlow/FilterDrawer.tsx
git commit -m "feat(platform): add platform visual adaptation tokens and component styles"
```

---

## Task 6: 状态栏插件集成

**Files:**
- Modify: `package.json`
- Modify: `src/main.tsx`
- Modify: `capacitor.config.ts`
- Test: `src/lib/__tests__/nativePlugins.test.ts`

**Interfaces:**
- Produces: `initNativePlugins()` utility that sets status bar style safely

- [ ] **Step 1: 安装插件**

Run:
```bash
npm install @capacitor/status-bar
npx cap sync
```

- [ ] **Step 2: 创建原生插件初始化函数**

Create `src/lib/nativePlugins.ts`:
```ts
import { StatusBar, Style } from '@capacitor/status-bar';

export async function initNativePlugins(): Promise<void> {
  try {
    await StatusBar.setStyle({ style: Style.Dark });
    await StatusBar.setBackgroundColor({ color: '#0a0e1a' });
  } catch {
    // Web preview or unsupported platform
  }
}
```

- [ ] **Step 3: 应用启动时调用**

Modify `src/main.tsx`:
```ts
import { initNativePlugins } from '@/lib/nativePlugins';

// After hydrate seed block or before render
void initNativePlugins();
```

- [ ] **Step 4: 添加测试**

Create `src/lib/__tests__/nativePlugins.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { initNativePlugins } from '@/lib/nativePlugins';

const setStyle = vi.fn();
const setBackgroundColor = vi.fn();

vi.mock('@capacitor/status-bar', () => ({
  StatusBar: { setStyle, setBackgroundColor },
  Style: { Dark: 'DARK' },
}));

describe('initNativePlugins', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('sets status bar to dark style and background color', async () => {
    await initNativePlugins();
    expect(setStyle).toHaveBeenCalledWith({ style: 'DARK' });
    expect(setBackgroundColor).toHaveBeenCalledWith({ color: '#0a0e1a' });
  });

  it('silently ignores errors on unsupported platforms', async () => {
    setStyle.mockRejectedValueOnce(new Error('not available'));
    await expect(initNativePlugins()).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 5: 运行测试**

```bash
npm run test:run -- src/lib/__tests__/nativePlugins.test.ts
```
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json src/main.tsx src/lib/nativePlugins.ts src/lib/__tests__/nativePlugins.test.ts capacitor.config.ts
git commit -m "feat(native): integrate Capacitor StatusBar plugin"
```

---

## Task 7: 安全区适配

**Files:**
- Create: `src/hooks/useSafeArea.ts`
- Modify: `src/index.css`
- Modify: `src/App.tsx`
- Test: `src/hooks/__tests__/useSafeArea.test.ts`

**Interfaces:**
- Produces: CSS variables `--sat`, `--sar`, `--sab`, `--sal` injected on root element
- Consumes: Tailwind arbitrary values / CSS custom properties in layout classes

- [ ] **Step 1: 创建安全区 hook**

Create `src/hooks/useSafeArea.ts`:
```ts
import { useEffect } from 'react';

export function useSafeArea(): void {
  useEffect(() => {
    const update = () => {
      const style = document.documentElement.style;
      style.setProperty('--sat', `${env(safe-area-inset-top)}`);
      style.setProperty('--sar', `${env(safe-area-inset-right)}`);
      style.setProperty('--sab', `${env(safe-area-inset-bottom)}`);
      style.setProperty('--sal', `${env(safe-area-inset-left)}`);
    };
    update();
    window.addEventListener('resize', update);
    return () => window.removeEventListener('resize', update);
  }, []);
}
```

- [ ] **Step 2: 在全局 CSS 中定义安全区变量兜底**

Modify `src/index.css`, add at top:
```css
:root {
  --sat: 0px;
  --sar: 0px;
  --sab: 0px;
  --sal: 0px;
}
```

- [ ] **Step 3: 在 App 中启用**

Modify `src/App.tsx`:
```ts
import { useSafeArea } from '@/hooks/useSafeArea';

export default function App() {
  useSafeArea();
  ...
}
```

- [ ] **Step 4: 应用安全区到布局**

Modify `src/App.tsx` main container:
```tsx
<main className="relative h-full overflow-hidden pb-[env(safe-area-inset-bottom)]">
```

Modify `src/components/BottomNav.tsx` to add `pb-[env(safe-area-inset-bottom)]`.

Modify `src/pages/LogStream/InputArea.tsx` fixed container to add `bottom-[calc(4rem+env(safe-area-inset-bottom))]` (or equivalent Tailwind).

- [ ] **Step 5: 添加测试**

Create `src/hooks/__tests__/useSafeArea.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { useSafeArea } from '@/hooks/useSafeArea';

function TestComponent() {
  useSafeArea();
  return <div />;
}

describe('useSafeArea', () => {
  it('sets safe-area CSS variables on document element', () => {
    render(<TestComponent />);
    const style = document.documentElement.style;
    expect(style.getPropertyValue('--sat')).toBe('env(safe-area-inset-top)');
    expect(style.getPropertyValue('--sab')).toBe('env(safe-area-inset-bottom)');
  });
});
```

- [ ] **Step 6: 运行测试**

```bash
npm run test:run -- src/hooks/__tests__/useSafeArea.test.ts
```

- [ ] **Step 7: Commit**

```bash
git add src/hooks/useSafeArea.ts src/hooks/__tests__/useSafeArea.test.ts src/index.css src/App.tsx src/components/BottomNav.tsx src/pages/LogStream/InputArea.tsx
git commit -m "feat(ui): apply safe-area insets to root, bottom nav and input area"
```

---

## Task 8: Android 返回键处理

**Files:**
- Create: `src/hooks/useBackButton.ts`
- Modify: `src/App.tsx`
- Test: `src/hooks/__tests__/useBackButton.test.ts`

**Interfaces:**
- Consumes: `currentPage` and `activeTab` from `useNavigationStore`
- Produces: `App.addListener('backButton', handler)` registration with cleanup

- [ ] **Step 1: 安装 App 插件**

Run:
```bash
npm install @capacitor/app
npx cap sync
```

- [ ] **Step 2: 创建返回键 hook**

Create `src/hooks/useBackButton.ts`:
```ts
import { useEffect } from 'react';
import { App } from '@capacitor/app';
import { useNavigationStore, type Page, TAB_PAGES, type Tab } from '@/stores/navigationStore';

export function useBackButton(exitApp: () => void): void {
  const { currentPage, activeTab, navigateTo } = useNavigationStore();

  useEffect(() => {
    const listener = App.addListener('backButton', ({ canGoBack }) => {
      if (!canGoBack) {
        exitApp();
        return;
      }
      if (TAB_PAGES.includes(currentPage as Tab)) {
        exitApp();
      } else {
        navigateTo(activeTab);
      }
    });

    return () => {
      listener.then((l) => l.remove());
    };
  }, [currentPage, activeTab, navigateTo, exitApp]);
}
```

- [ ] **Step 3: 在 App 中集成**

Modify `src/App.tsx`:
```ts
import { useBackButton } from '@/hooks/useBackButton';

export default function App() {
  useSafeArea();
  useBackButton(() => {
    // On Android, App.exitApp() is available via @capacitor/app
    import('@capacitor/app').then(({ App }) => App.exitApp()).catch(() => {});
  });
  ...
}
```

- [ ] **Step 4: 添加测试**

Create `src/hooks/__tests__/useBackButton.test.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render } from '@testing-library/react';
import { useBackButton } from '@/hooks/useBackButton';
import { useNavigationStore } from '@/stores/navigationStore';

const removeListener = vi.fn();
const addListener = vi.fn(() => Promise.resolve({ remove: removeListener }));
const exitApp = vi.fn();

vi.mock('@capacitor/app', () => ({
  App: { addListener, exitApp },
}));

function TestComponent() {
  useBackButton(exitApp);
  return <div />;
}

describe('useBackButton', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    useNavigationStore.setState({ currentPage: 'log', activeTab: 'log' });
  });

  it('registers back button listener', () => {
    render(<TestComponent />);
    expect(addListener).toHaveBeenCalledWith('backButton', expect.any(Function));
  });

  it('exits app on tab home page', async () => {
    render(<TestComponent />);
    const handler = addListener.mock.calls[0][1];
    handler({ canGoBack: true });
    await Promise.resolve();
    expect(exitApp).toHaveBeenCalled();
  });

  it('navigates to active tab on non-tab page', async () => {
    useNavigationStore.setState({ currentPage: 'settings', activeTab: 'log' });
    render(<TestComponent />);
    const handler = addListener.mock.calls[0][1];
    handler({ canGoBack: true });
    await Promise.resolve();
    expect(useNavigationStore.getState().currentPage).toBe('log');
    expect(exitApp).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 5: 运行测试**

```bash
npm run test:run -- src/hooks/__tests__/useBackButton.test.ts
```

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json src/hooks/useBackButton.ts src/hooks/__tests__/useBackButton.test.ts src/App.tsx
git commit -m "feat(android): handle physical back button with App plugin"
```

---

## Task 9: 键盘插件集成

**Files:**
- Modify: `package.json`
- Modify: `src/lib/nativePlugins.ts`
- Modify: `capacitor.config.ts`
- Test: `src/lib/__tests__/nativePlugins.test.ts`

**Interfaces:**
- Produces: `Keyboard.setResizeMode({ mode: 'ionic' })` call on startup

- [ ] **Step 1: 安装插件**

Run:
```bash
npm install @capacitor/keyboard
npx cap sync
```

- [ ] **Step 2: 更新 nativePlugins.ts**

Modify `src/lib/nativePlugins.ts`:
```ts
import { StatusBar, Style } from '@capacitor/status-bar';
import { Keyboard, KeyboardResize } from '@capacitor/keyboard';

export async function initNativePlugins(): Promise<void> {
  try {
    await StatusBar.setStyle({ style: Style.Dark });
    await StatusBar.setBackgroundColor({ color: '#0a0e1a' });
  } catch {
    // Web preview or unsupported platform
  }

  try {
    await Keyboard.setResizeMode({ mode: KeyboardResize.Ionic });
  } catch {
    // Web preview or unsupported platform
  }
}
```

- [ ] **Step 3: 更新测试**

Modify `src/lib/__tests__/nativePlugins.test.ts`:
```ts
const setResizeMode = vi.fn();

vi.mock('@capacitor/keyboard', () => ({
  Keyboard: { setResizeMode },
  KeyboardResize: { Ionic: 'ionic' },
}));

it('sets keyboard resize mode', async () => {
  await initNativePlugins();
  expect(setResizeMode).toHaveBeenCalledWith({ mode: 'ionic' });
});
```

- [ ] **Step 4: 运行测试**

```bash
npm run test:run -- src/lib/__tests__/nativePlugins.test.ts
```

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json src/lib/nativePlugins.ts src/lib/__tests__/nativePlugins.test.ts capacitor.config.ts
git commit -m "feat(native): integrate Capacitor Keyboard resize mode"
```

---

## Task 10: 主题模式状态与设置入口

**Files:**
- Create: `src/stores/themeStore.ts`
- Modify: `src/pages/Settings/index.tsx`
- Modify: `src/App.tsx`
- Test: `src/stores/__tests__/themeStore.test.ts`

**Interfaces:**
- Produces: `useThemeStore` with `mode: 'system' | 'dark' | 'light'` and `resolvedTheme: 'dark' | 'light'`
- Produces: `ThemeToggle` component or inline UI in Settings

- [ ] **Step 1: 创建 theme store**

Create `src/stores/themeStore.ts`:
```ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

export type ThemeMode = 'system' | 'dark' | 'light';

interface ThemeState {
  mode: ThemeMode;
  setMode: (mode: ThemeMode) => void;
  resolvedTheme: () => 'dark' | 'light';
}

function getSystemTheme(): 'dark' | 'light' {
  if (typeof window === 'undefined') return 'dark';
  return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
}

export const useThemeStore = create<ThemeState>()(
  persist(
    (set, get) => ({
      mode: 'system',
      setMode: (mode) => set({ mode }),
      resolvedTheme: () => {
        const mode = get().mode;
        return mode === 'system' ? getSystemTheme() : mode;
      },
    }),
    {
      name: 'flash-theme',
      version: 1,
      partialize: (state) => ({ mode: state.mode }),
    }
  )
);
```

- [ ] **Step 2: 在 App 中监听系统主题变化**

Modify `src/App.tsx`:
```ts
import { useEffect } from 'react';
import { useThemeStore } from '@/stores/themeStore';

export default function App() {
  useSafeArea();
  useBackButton(...);
  const mode = useThemeStore((s) => s.mode);

  useEffect(() => {
    if (mode !== 'system') return;
    const mql = window.matchMedia('(prefers-color-scheme: dark)');
    const handler = () => {
      // Force re-render by touching store state
      useThemeStore.setState({ mode: 'system' });
    };
    mql.addEventListener('change', handler);
    return () => mql.removeEventListener('change', handler);
  }, [mode]);
  ...
}
```

- [ ] **Step 3: 在 Settings 添加主题切换**

Modify `src/pages/Settings/index.tsx`:
```tsx
import { useThemeStore, type ThemeMode } from '@/stores/themeStore';

const THEME_LABELS: Record<ThemeMode, string> = {
  system: '跟随系统',
  dark: '始终深色',
  light: '始终浅色',
};

export default function Settings() {
  const { mode, setMode } = useThemeStore();
  ...
}
```

Add a new section in Settings UI:
```tsx
<section>
  <h2 className="text-xs font-medium text-slate-500 uppercase tracking-wider mb-2 px-1">外观</h2>
  <LiquidGlassCard>
    <div className="divide-y divide-white/10">
      {(['system', 'dark', 'light'] as ThemeMode[]).map((m) => (
        <label key={m} className="w-full flex items-center gap-3 py-3.5 cursor-pointer">
          <input
            type="radio"
            name="theme"
            checked={mode === m}
            onChange={() => setMode(m)}
            className="mt-0.5"
          />
          <span className="text-sm text-white">{THEME_LABELS[m]}</span>
        </label>
      ))}
    </div>
  </LiquidGlassCard>
</section>
```

- [ ] **Step 4: 添加测试**

Create `src/stores/__tests__/themeStore.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { useThemeStore } from '@/stores/themeStore';

describe('themeStore', () => {
  beforeEach(() => {
    localStorage.clear();
    useThemeStore.setState({ mode: 'system' }, true);
  });

  it('defaults to system mode', () => {
    expect(useThemeStore.getState().mode).toBe('system');
  });

  it('persists selected mode', () => {
    useThemeStore.getState().setMode('dark');
    expect(useThemeStore.getState().mode).toBe('dark');
  });

  it('resolves dark when mode is dark', () => {
    useThemeStore.setState({ mode: 'dark' });
    expect(useThemeStore.getState().resolvedTheme()).toBe('dark');
  });
});
```

- [ ] **Step 5: 运行测试**

```bash
npm run test:run -- src/stores/__tests__/themeStore.test.ts
```

- [ ] **Step 6: Commit**

```bash
git add src/stores/themeStore.ts src/stores/__tests__/themeStore.test.ts src/pages/Settings/index.tsx src/App.tsx
git commit -m "feat(theme): add theme mode store and settings toggle"
```

---

## Task 11: 深色模式全局检查与修复

**Files:**
- Modify: `src/index.css`
- Modify: `src/pages/Calendar/index.tsx`
- Modify: `src/pages/LogFlow/index.tsx`
- Modify: `src/pages/LogFlow/FilterDrawer.tsx`
- Modify: `src/pages/Settings/index.tsx`
- Modify: `src/pages/CurrentEmotion/StatsPanel.tsx`
- Test: visual regression via existing component tests + manual smoke

**Interfaces:**
- Produces: consistent dark-themed UI across all pages

- [ ] **Step 1: 扫描硬编码颜色**

Run:
```bash
grep -R "#FFF\|#fff\|#000\|#000000\|rgb(255\|rgb(0," src/pages src/components --include="*.tsx" --include="*.css"
```
Replace any remaining hard-coded light/dark colors with Tailwind slate/white classes or CSS variables.

- [ ] **Step 2: 修复 Calendar 选中态与周末标记**

Ensure `CalendarDay` uses `text-white` for current month and `text-slate-600` for other months. Verify selected state background uses `bg-white/10`.

- [ ] **Step 3: 修复 FilterDrawer 输入框与下拉框**

Ensure `<select>` dropdown options are readable on dark background. Add `bg-slate-900 text-white` to select options if needed (browser rendering may vary).

- [ ] **Step 4: 修复 Settings 输入/抽屉**

Ensure export/import drawers, radio inputs, and issue warning boxes have sufficient contrast.

- [ ] **Step 5: 修复 StatsPanel 图表 tooltip**

Tooltip already uses `rgba(15, 23, 42, 0.9)`; verify on dark background.

- [ ] **Step 6: 全局兜底样式**

Add to `src/index.css`:
```css
html {
  background-color: #0a0e1a;
}
body {
  background-color: #0a0e1a;
  color: #ffffff;
}
```

- [ ] **Step 7: 运行全量测试与构建**

```bash
npm run lint
npm run format:check
npx tsc -b --noEmit
npm run test:run
npm run build
```

- [ ] **Step 8: Commit**

```bash
git add src/index.css src/pages/Calendar/index.tsx src/pages/LogFlow/index.tsx src/pages/LogFlow/FilterDrawer.tsx src/pages/Settings/index.tsx src/pages/CurrentEmotion/StatsPanel.tsx
git commit -m "fix(theme): audit and fix dark mode consistency across pages"
```

---

## Task 12: 最终集成与验证

**Files:**
- Modify: any remaining build/test config
- Create: `docs/superpowers/plans/2026-07-13-phase3-completion.md` (optional notes)

**Interfaces:**
- Produces: signed release APK/AAB, iOS project ready for signing

- [ ] **Step 1: 全量验证命令**

Run:
```bash
npm run lint
npm run format:check
npx tsc -b --noEmit
npm run test:run
npm run build
```
Expected: All green.

- [ ] **Step 2: Android release 构建**

Run:
```bash
npx cap sync android
cd android && ./gradlew assembleRelease && ./gradlew bundleRelease
```
Expected: APK and AAB produced and signed.

- [ ] **Step 3: iOS 同步检查**

Run:
```bash
npx cap sync ios
```
Expected: No errors. iOS signing warnings are expected.

- [ ] **Step 4: 真机/模拟器 smoke test**

Install release APK on Android emulator/device:
```bash
adb install -r android/app/build/outputs/apk/release/app-release.apk
```
Verify:
- Splash 正常隐藏
- 底部导航可见
- 可记录日志
- 返回键行为正确
- 深色模式切换有效
- Android 平台视觉（Material 3 圆角/阴影/Indigo 强调色）生效

For iOS, build in Xcode Simulator and verify Liquid Glass 风格（较小圆角、Apple 蓝强调色、强 backdrop blur）生效.

- [ ] **Step 5: 更新 ROADMAP.md Phase 3 状态**

Mark completed tasks in `app/ROADMAP.md` Phase 3 table:
- Android 发布配置 → 已完成
- iOS 发布配置 → 基线完成（签名待后续）
- 状态栏/安全区适配 → 已完成
- 返回键/手势适配 → 已完成
- 添加 Settings 页面 → 已完成（Phase 2）
- 深色模式完整适配 → 已完成
- 平台视觉适配 → 已完成

- [ ] **Step 6: 最终提交并推送**

```bash
git add -A
git commit -m "feat(phase3): complete mobile release preparation"
git push origin main
```

---

## Self-Review

**Spec coverage:**
- Android release signing → Task 1
- versionCode/versionName automation → Task 2
- minify/ProGuard → Task 3
- iOS baseline + icons/launch screen → Task 4
- Platform visual adaptation (section 2) → Task 5
- StatusBar plugin → Task 6
- Safe area → Task 7
- Back button → Task 8
- Keyboard resize → Task 9
- Dark mode + theme toggle → Tasks 10-11
- Final verification → Task 12

**Placeholder scan:**
- No TBD/TODO in concrete steps.
- Code blocks contain actual expected code.
- Exact file paths specified.

**Type consistency:**
- `ThemeMode` type reused in store and Settings.
- `useSafeArea`, `useBackButton` hooks have consistent signatures.
- Capacitor plugin imports match installed packages.
- `Platform` type and `getPlatform()` return values match CSS class names.

**Open risks:**
- iOS 工程首次打开 Xcode 需要手动处理签名警告，属于预期范围。
- ProGuard 规则可能需要根据 release 构建崩溃日志进一步补充。
- 自定义 iOS 图标与启动图留待后续正式设计稿阶段完成。
- Jetpack Compose / SwiftUI 等原生 UI 框架引入已明确搁置到 Phase 5，Phase 3 仅通过 CSS/Tailwind 实现平台视觉差异。
